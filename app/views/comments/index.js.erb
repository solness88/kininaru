id = <%= @new.id %>
$("#comments_area" + id).html("<%= j(render 'comments/index', { comments: @comment.new.comments, new: @comment.new }) %>");
$("textarea").val('');
$("#notice").html("<%= j flash[:notice] %>");
