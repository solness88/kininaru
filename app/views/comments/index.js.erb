id = <%= @article.id %>
$("#comments_area" + id).html("<%= j(render 'comments/index', { comments: @comment.article.comments, article: @comment.article }) %>");
$("textarea").val('');
$("#notice").html("<%= j flash[:notice] %>");
