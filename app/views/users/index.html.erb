<div class="toppage_kininaru col-sm-12 col-md-3 container">
  <h2>このチームのメンバー</h2>
  <% @users.each do |user| %>
  <div class="user_index row">
    <div class="col-sm-1"></div>
    <div><%= image_tag user.image.url, class:"user_image" %></div>
    <div class="user_name col-sm-6"><%= link_to user.name, user_path(user.id) %></div>
    <div class="col-sm-1"></div>
  </div>
  <% end %>
</div>
